// ==========================
// ЗМІННІ: let і const
// ==========================
/*
// let — змінна, яку можна змінити
let age = 18;
console.log("Вік:", age); // Виведе: Вік: 18

age = 19; // змінили значення
console.log("Новий вік:", age); // Виведе: Новий вік: 19

// const — постійна змінна, не можна змінювати
const name = "Святослав";
console.log("Ім'я:"+ name); // Виведе: Ім'я: Святослав

// name = "Інше"; // ❌ Помилка! const не можна змінювати



// ==========================
// 🔢 ТИПИ ДАНИХ
// ==========================

let message = "Привіт";          // string (рядок)
let number = 42;                 // number (число)
let isStudent = true;           // boolean (істина/хиба)
let fruits = [9, "банан"]; // масив
let user = {                    // об'єкт
  name: "Святослав",
  age: 21
};

console.log("Типи:", message, number, isStudent, fruits, user);



// ==========================
// ⚖️ if / else
// ==========================

let mark = 85;

if (mark >= 90) {
  console.log("Оцінка: Відмінно");
} else if (mark >= 75) {
  console.log("Оцінка: Добре");
} else {
  console.log("Оцінка: Потрібно підтягнутись");
}



// ==========================
// ❓ ТЕРНАРНИЙ ОПЕРАТОР
// ==========================

let temperature = 30;

let weather = temperature > 25 ? "Спекотно" : "Прохолодно";//умова ? якщо_істина : якщо_хиба;

console.log("Погода:", weather);



// ==========================
// МЕТОДИ ДЛЯ МАСИВІВ
// ==========================

let numbers = [1, 2, 3, 4, 5]; // створили масив чисел

// .map() — змінює кожен елемент масиву
let doubled = numbers.map(function(n) {
  return n * 2;        // кожне число × 2
});
console.log("Помножені на 2:", doubled); // [2, 4, 6, 8, 10]

// .filter() — залишає лише ті, що проходять умову
let even = numbers.filter(n => n % 2 === 0); // залишити, якщо число парне
console.log("Парні числа:", even); // [2, 4]

// .find() — знаходить ПЕРШИЙ елемент, що підходить
let found = numbers.find(n => n > 3); // знайти перше число > 3
console.log("Перше число > 3:", found); // 4




// ==========================
// 🧠 ТРЕНУВАЛЬНІ ЗАДАЧКИ
// ==========================

// 1. Якщо число парне — "парне", інакше "непарне"
let testNumber = 7;
let parity = testNumber % 2 === 0 ? "парне" : "непарне";//перевірка залишку від ділення на 2
console.log(testNumber, "—", parity);

// 2. Логін і пароль
let login = "admin";
let password = "1234";

if (login === "admin" && password === "1234") {
  console.log("Успішний вхід");
} else {
  console.log("Доступ заборонено");
}

// 3. З масиву [4, 9, 12, 3] — вибрати числа > 5
let data = [4, 9, 12, 3];
let moreThanFive = data.filter(n => n > 5);
// .filter() перебирає всі числа в масиві
// n => n > 5 → залишає тільки ті числа, які більше 5
// результат — новий масив: [9, 12]
console.log("Числа > 5:", moreThanFive);


*/
                                                 //День 2

/*
for (старт; умова; крок) {
   тіло циклу
}
*/
// Вивести числа від 1 до 5
for (let i = 1; i <= 5; i++) { // цикл: починаємо з 1, поки i ≤ 5, кожен крок +1
  console.log("Число:", i);   // виводимо текст і значення i
}


// Порахувати суму чисел від 1 до 3
let sum = 0; // початкова сума — 0
for (let i = 1; i <= 3; i++) { // цикл від 1 до 3
  sum += i; // додаємо i до sum (sum = sum + i)
}
console.log("Сума:", sum); // виводимо результат: 6


let fruits = ["яблуко", "банан", "вишня"];

for (let fruit of fruits) {
// for — цикл для перебору
// (let fruit of fruits) — кожен елемент з масиву fruits записується у fruit

  console.log("Фрукт:", fruit);
// console — об’єкт для виводу
// .log — метод виводу
// "Фрукт:" — текст
// , fruit — значення з масиву
}





let numbers = [10, 20, 30];

numbers.forEach((num, index) => {
// forEach — метод, що виконує функцію для кожного елемента
// (num, index) — num: значення, index: позиція в масиві
// => — стрілкова функція (анонімна)
  console.log(index, "→", num);
// виводимо індекс і число
}); 

/*Стрілкова функція (arrow function) — це спрощений спосіб написання функцій у JavaScript, який виглядає коротше, 
ніж звичайна function. Вона з’явилась у новій версії JavaScript (ES6) і часто використовується через свою компактність.*/ 


//.map() — перетворює елементи масиву

let nums = [1, 2, 3];            
let squared = nums.map(n => n * n); // створюємо нову змінну squared
                                     // .map() — метод масиву, який змінює кожен елемент
                                     // n => n * n — стрілкова функція:
                                     // n — кожне число з масиву
                                     // n * n — піднести до квадрату
                                     // результат: [1, 4, 9]
let greetings = ["Святослав", "Олег"].map(name => "Привіт, " + name);
// ["Святослав", "Олег"] — масив імен
// .map(...) — пройдемось по кожному імені
// name => "Привіт, " + name — додаємо до кожного слова "Привіт, "
// + — оператор додавання (тут об'єднує строки)
// результат: ["Привіт, Святослав", "Привіт, Олег"]
console.log(greetings); // виведе масив привітань у консоль





// .filter() — залишає тільки ті, що проходять умову

let arr = [5, 10, 15, 20];           // масив із чисел
let big = arr.filter(n => n >= 15); // створюємо масив з елементів, де n >= 15
                                     // n => n >= 15 — умова: залишити, якщо n більше або дорівнює 15
                                     // результат: [15, 20]
let names = ["Анна", "Олег", "Іра"].filter(name => name.length <= 3);
// .filter(...) — відфільтруємо імена
// name.length — довжина кожного імені
// <= 3 — залишити, якщо довжина ≤ 3
// результат: ["Іра"]
console.log(names); // виведе: ["Іра"]




//.find() — знаходить перший елемент, що підходить

let numbers2 = [2, 4, 6, 7, 8];         // масив чисел
let firstOdd = numbers2.find(n => n % 2 !== 0);
// .find(...) — шукає перше число, яке задовольняє умову
// n % 2 !== 0 — перевірка: число непарне
// % — залишок від ділення
// !== — не дорівнює
// результат: 7 (перше непарне)
let words = ["кіт", "каша", "корова"];     // масив слів
let kasha = words.find(word => word === "каша");
// знайти перше слово, яке точно дорівнює "каша"
// === — суворе порівняння
// результат: "каша"





// Тернарний оператор умова ? якщо_так : якщо_ні

let age = 17;                           // змінна з віком
let access = age >= 18 ? "Дозволено" : "Заборонено";
// тернарний оператор:
// якщо age >= 18 → "Дозволено"
// інакше → "Заборонено"
// результат: "Заборонено", бо 17 < 18
let temp = 5;                             // температура
let jacket = temp < 10 ? "Одягни куртку" : "Не треба куртки";
// якщо температура менше 10 → "Одягни куртку"
// інакше → "Не треба куртки"
// результат: "Одягни куртку", бо 5 < 10





